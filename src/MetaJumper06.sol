pragma solidity 0.6.6;

contract MetaJumper06 {
    function metaJump() public {
        function() internal fn;
        assembly {
            fn := sub(codesize(), 0x0f)
        }
        fn();
    }
}
